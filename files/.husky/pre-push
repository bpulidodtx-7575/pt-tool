# The real gate. These are the same checks CI runs, so a red build should be
# something you already saw locally.
cd files || exit 1
npm run typecheck &&
  npm run lint &&
  npm run format:check &&
  npm run test:coverage &&
  npm run deadcode &&
  npm run slop:ci
