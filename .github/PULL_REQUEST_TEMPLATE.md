## What changed

<!-- The change itself, in a sentence or two. -->

## Verification

<!-- Not "tests pass" — what you actually ran, and what it printed.
     Paste the command and the result. -->

- [ ] `npm run lint && npm run format:check && npm run typecheck`
- [ ] `npm run test:coverage`
- [ ] `npm run deadcode && npm run slop:ci`
- [ ] `npm run build`

## Unverified / assumed

<!-- The most important box on this form. What did you NOT check?
     What is assumed to hold rather than known to?
     "Nothing" is a valid answer, but say it deliberately. -->

## Gate movement

<!-- If a threshold changed (coverage, aislop failBelow), say which way and why.
     Lowering a gate to make a build pass needs a reason here, not a commit message. -->
